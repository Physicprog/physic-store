{

  "homepage" : "https://Physicprog.github.io/physic-store",
  "name": "digital-store",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "react-scripts start",
    "predeploy": "npm run build",
    "desploy": "gh-pages -d build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"

  },
  "dependencies": {
    "@stripe/stripe-js": "^2.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
